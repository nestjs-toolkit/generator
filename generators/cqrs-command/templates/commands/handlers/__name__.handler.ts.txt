import { Logger } from '@nestjs/common';
import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import clc from 'cli-color';
import { <%= namePascal %>Command } from '../impl';

@CommandHandler(<%= namePascal %>Command)
export class <%= namePascal %>Handler
  implements ICommandHandler<<%= namePascal %>Command, void> {
  private logger = new Logger(<%= namePascal %>Handler.name);


  execute(command: <%= namePascal %>Command): Promise<void> {
    this.logger.debug(clc.greenBright(`Init `));
  }
}
