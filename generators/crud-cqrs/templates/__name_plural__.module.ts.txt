import { Module } from '@nestjs/common';
import { CqrsModule } from '@nestjs/cqrs';
import { MongooseModule } from '@nestjs/mongoose';
import { CommandHandlers } from './commands/handlers';
import { EventHandlers } from './events/handlers';
import { QueryHandlers } from './queries/handlers';
import { SagaListeners } from './sagas';
import { <%= namePluralPascal %>ManagerResolver, <%= namePluralPascal %>UserResolver } from './resolvers';
import { <%= namePascal %>, <%= namePascal %>Schema } from './schemas';
import { <%= namePluralPascal %>Repository } from './<%= namePlural %>.repository';

@Module({
  imports: [
    CqrsModule,
    MongooseModule.forFeature([{ name: <%= namePascal %>.name, schema: <%= namePascal %>Schema }]),
  ],
  providers: [
    <%= namePluralPascal %>UserResolver,
    <%= namePluralPascal %>ManagerResolver,
    <%= namePluralPascal %>Repository,
    ...CommandHandlers,
    ...EventHandlers,
    ...QueryHandlers,
    ...SagaListeners,
  ],
})
export class <%= namePluralPascal %>Module {}
