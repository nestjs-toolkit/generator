import { ContextType } from '@core/types';
import { <%= namePascal %>FilterDto, <%= namePascal %>UpdateDto } from '../../dto';

export class UpdateMany<%= namePascal %>Command {
  constructor(
    public readonly filter: <%= namePascal %>FilterDto,
    public readonly update: <%= namePascal %>UpdateDto,
    public readonly context: ContextType,
  ) {}
}
