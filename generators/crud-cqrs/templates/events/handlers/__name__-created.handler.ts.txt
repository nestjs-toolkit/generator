import { EventsHandler, IEventHandler } from '@nestjs/cqrs';
import { WinstonLogger } from '@nestjs-toolkit/winston-logger';
import { <%= namePascal %>CreatedEvent } from '../impl';

// TODO refatorar para registrar LOG e enviar para logstash + kibana
@EventsHandler(<%= namePascal %>CreatedEvent)
export class <%= namePascal %>CreatedHandler implements IEventHandler<<%= namePascal %>CreatedEvent> {
  constructor(private readonly logger: WinstonLogger) {}

  handle({ model, dto, context }: <%= namePascal %>CreatedEvent) {
    const { req, user } = context;
    const causer = {
      collection: 'user_keycloak',
      username: user.preferred_username,
      _id: user.sub,
    };

    this.logger
      .activity()
      .tags('<%= namePlural %>')
      .action('<%= namePlural %>:create')
      .requestGql(req)
      .causedBy(causer)
      .performedOn(model, '<%= namePlural %>')
      .namespaceById('*', 'manager')
      .withProperty('dto', dto)
      .withProperty('model', model.toJSON())
      .contextIn(<%= namePascal %>CreatedHandler.name)
      .log('Cadastrado <%= name %>: :subject.name');
  }
}
