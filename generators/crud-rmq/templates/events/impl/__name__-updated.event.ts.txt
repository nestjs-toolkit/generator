import { ContextType } from '@core/types';
import { <%= namePascal %> } from '../../schemas';
import { <%= namePascal %>UpdateDto } from '../../dto';

export class <%= namePascal %>UpdatedEvent {
  constructor(
    public readonly model: <%= namePascal %>,
    public readonly old: Record<string, any>,
    public readonly dto: <%= namePascal %>UpdateDto,
    public readonly context: ContextType,
  ) {}
}
